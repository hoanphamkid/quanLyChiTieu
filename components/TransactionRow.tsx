import {Text,View} from 'react-native'; import {colors} from '../constants/colors'; import {money} from '../utils/currency'; import type {Transaction} from '../types/models'; export function TransactionRow({item}:{item:Transaction}){return <View style={{flexDirection:'row',alignItems:'center',paddingVertical:11}}><Text style={{fontSize:24,width:42}}>{item.icon}</Text><View style={{flex:1}}><Text style={{color:colors.text,fontWeight:'600'}}>{item.note||item.category}</Text><Text style={{color:colors.muted,fontSize:12,marginTop:4}}>{item.wallet}</Text></View><Text style={{color:item.type==='income'?colors.income:colors.expense,fontWeight:'700'}}>{item.type==='income'?'+':'-'}{money(item.amount)}</Text></View>}
