import {ScrollView,Text,View} from 'react-native'; import {colors} from '../../constants/colors'; import {useFinanceStore} from '../../store/useFinanceStore'; import {money} from '../../utils/currency'; export default function Wallets(){const ws=useFinanceStore(s=>s.wallets);return <ScrollView style={{backgroundColor:colors.background}} contentContainerStyle={{padding:22,paddingTop:62,paddingBottom:110}}><Text style={{color:colors.text,fontSize:30,fontWeight:'800'}}>Ví của tôi</Text><View style={{backgroundColor:colors.primary,borderRadius:24,padding:22,marginVertical:24}}><Text style={{color:'#17304a'}}>Tổng tài sản</Text><Text style={{fontSize:28,fontWeight:'800',marginTop:8}}>{money(ws.reduce((a,w)=>a+w.balance,0))}</Text></View>{ws.map(w=><View key={w.id} style={{backgroundColor:colors.card,borderRadius:18,padding:18,marginBottom:12,flexDirection:'row'}}><Text style={{fontSize:26}}>{w.icon}</Text><View style={{marginLeft:14}}><Text style={{color:colors.text,fontWeight:'700'}}>{w.name}</Text><Text style={{color:colors.muted,marginTop:5}}>{money(w.balance)}</Text></View></View>)}</ScrollView>}
