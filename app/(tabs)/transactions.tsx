import {ScrollView,Text,View,TextInput} from 'react-native'; import {colors} from '../../constants/colors'; import {useFinanceStore} from '../../store/useFinanceStore'; import {TransactionRow} from '../../components/TransactionRow'; export default function Transactions(){const ts=useFinanceStore(s=>s.transactions);return <ScrollView style={{flex:1,backgroundColor:colors.background}} contentContainerStyle={{padding:22,paddingTop:62,paddingBottom:110}}><Text style={{color:colors.text,fontSize:30,fontWeight:'800'}}>Giao dịch</Text><TextInput placeholder="Tìm giao dịch..." placeholderTextColor={colors.muted} style={{backgroundColor:colors.surface,color:colors.text,borderRadius:14,padding:15,marginVertical:22}}/><View style={{backgroundColor:colors.card,borderRadius:20,padding:16}}>{ts.map(t=><TransactionRow key={t.id} item={t}/>)}</View></ScrollView>}
