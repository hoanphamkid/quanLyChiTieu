import {View,Text,TextInput,TouchableOpacity} from 'react-native'; import {router} from 'expo-router'; import {colors} from '../../constants/colors'; import {useFinanceStore} from '../../store/useFinanceStore'; export default function Add(){const add=useFinanceStore(s=>s.add);return <View style={{flex:1,backgroundColor:colors.background,padding:22,paddingTop:62}}><Text style={{color:colors.text,fontSize:28,fontWeight:'800'}}>Thêm giao dịch</Text><Text style={{color:colors.muted,marginTop:30}}>Số tiền</Text><TextInput keyboardType="numeric" placeholder="0đ" placeholderTextColor={colors.muted} style={{color:colors.text,fontSize:42,fontWeight:'800',borderBottomWidth:1,borderBottomColor:colors.border,paddingVertical:12}}/><TextInput placeholder="Ghi chú" placeholderTextColor={colors.muted} style={{backgroundColor:colors.surface,color:colors.text,padding:16,borderRadius:14,marginTop:26}}/><TouchableOpacity onPress={()=>{add({id:Date.now().toString(),type:'expense',amount:0,category:'Khác',icon:'📦',wallet:'Tiền mặt',note:'Giao dịch mới',date:new Date().toISOString()});router.back()}} style={{backgroundColor:colors.primary,padding:18,borderRadius:16,alignItems:'center',marginTop:30}}><Text style={{fontWeight:'800'}}>Lưu giao dịch</Text></TouchableOpacity></View>}
